--!strict
--!optimize 2
--!native

return {
	addressResolver = require(script.addressResolver),
	dictionary = require(script.dictionary),
	guard = require(script.guard),
	sample = require(script.sample),
	text = require(script.text),
}
